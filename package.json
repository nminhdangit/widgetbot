{
  "name": "widgetbot",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "./build-all.sh",
    "pull": "git stash && git pull && yarn && yarn build",
    "precommit": "pretty-quick --staged",
    "test": "yarn build"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "lerna": "~3.1.4"
  },
  "heroku-run-build-script": true
}
